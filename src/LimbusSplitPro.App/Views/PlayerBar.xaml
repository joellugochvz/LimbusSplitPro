<UserControl x:Class="LimbusSplitPro.App.Views.PlayerBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800">
    <Border Background="#0D0D14" BorderBrush="#1E2230" BorderThickness="0,1,0,0" Padding="16,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top Row: Timeline Seek Slider -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Slider Grid.Column="0" Minimum="0" Maximum="{Binding TotalDurationSeconds, FallbackValue=100}" Value="{Binding SeekSliderValue, Mode=TwoWay}" ValueChanged="OnSeekValueChanged" VerticalAlignment="Center" Margin="0,0,14,0" />
                <TextBlock Grid.Column="1" Text="{Binding FormattedTimeText}" Foreground="#6B7280" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center" FontFamily="Consolas" />
            </Grid>

            <!-- Bottom Row: Transport & Status -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Transport Controls -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding PlayPauseCommand}" Width="36" Height="36" Padding="0" Margin="0,0,6,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#00F0FF" CornerRadius="18">
                                    <TextBlock Text="{Binding IsPlaying, Converter={StaticResource PlayPauseIconConverter}, FallbackValue='▶'}" FontSize="14" Foreground="#0A0A0F" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Command="{Binding StopCommand}" Style="{StaticResource FluentSecondaryButtonStyle}" Width="36" Height="36" Padding="0">
                        <TextBlock Text="⏹" FontSize="14" />
                    </Button>
                </StackPanel>

                <!-- Status Messages -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                    <TextBlock Text="{Binding CurrentStageText}" Foreground="White" FontSize="11" TextTrimming="CharacterEllipsis" />
                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding ProcessingProgress}" Height="3" Margin="0,4,0,0" 
                                 Foreground="#00F0FF" Background="#1E2230" BorderThickness="0"
                                 Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#6B7280" FontSize="10" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <!-- Export & Folder Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="💾 Exportar Mezcla" Style="{StaticResource FluentSecondaryButtonStyle}" Command="{Binding ExportMixCommand}" Padding="10,6" FontSize="11" Margin="0,0,6,0" />
                    <Button Content="📁 Abrir Carpeta" Style="{StaticResource FluentSecondaryButtonStyle}" Command="{Binding OpenOutputFolderCommand}" Padding="10,6" FontSize="11" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
